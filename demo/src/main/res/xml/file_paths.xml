<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the app's own external files directory is exposed, which is where the
         trace is written. Nothing else on the device is shareable through this provider. -->
    <external-files-path
        name="andropilot_traces"
        path="." />
</paths>
