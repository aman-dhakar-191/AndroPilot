.gradle/
build/
local.properties
*.iml
.idea/
.kotlin/
captures/
*.apk
*.aab

# The host's shared token, generated by scripts/run-host.sh on first run and reused so the
# phone does not need reconfiguring after every restart. It is a credential for a socket
# that can drive a phone; it does not belong in the repository.
.andropilot/

