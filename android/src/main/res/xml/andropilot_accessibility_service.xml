<?xml version="1.0" encoding="utf-8"?>
<!--
  Event types are kept narrow on purpose. The driver reads the tree on demand rather than
  reacting to every event, so subscribing to the full firehose would only cost battery and
  main-thread time. WINDOW_STATE_CHANGED and WINDOW_CONTENT_CHANGED are what the
  change-detection layer actually uses.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows|flagIncludeNotImportantViews"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/andropilot_service_description"
    android:notificationTimeout="100" />
